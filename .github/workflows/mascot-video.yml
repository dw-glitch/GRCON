name: Validação do mascote em vídeo

on:
  push:
    branches: [main]
    paths:
      - "assets/mascot/video/**"
      - "grcon-mascot-sprite.png"
      - "grcon_mascot_controller.js"
      - "grcon_mascot_controller_v4.js"
      - "grcon_mascot_runner.js"
      - "grcon_mascot_success_pilot.js"
      - "egrdt_teams_notification_app.js"
      - "sw.js"
      - "index.html"
      - "vercel.json"
      - "scripts/validar-mascote-video-browser.cjs"
      - "scripts/validar-mascote-success-pilot-browser.cjs"
      - "scripts/validar-consultas-browser.cjs"
      - "src/react/consultas/**"
      - "requests-phase-b.css"
      - "react-ui.css"
      - "tests/fixtures/grcon-mascot-video.html"
      - "tests/grcon_mascot_video.cjs"
      - "tests/grcon_mascot_success_pilot.cjs"
      - ".github/workflows/mascot-video.yml"
  pull_request:
    paths:
      - "assets/mascot/video/**"
      - "grcon-mascot-sprite.png"
      - "grcon_mascot_controller.js"
      - "grcon_mascot_controller_v4.js"
      - "grcon_mascot_runner.js"
      - "grcon_mascot_success_pilot.js"
      - "egrdt_teams_notification_app.js"
      - "sw.js"
      - "index.html"
      - "vercel.json"
      - "scripts/validar-mascote-video-browser.cjs"
      - "scripts/validar-mascote-success-pilot-browser.cjs"
      - "scripts/validar-consultas-browser.cjs"
      - "src/react/consultas/**"
      - "requests-phase-b.css"
      - "react-ui.css"
      - "tests/fixtures/grcon-mascot-video.html"
      - "tests/grcon_mascot_video.cjs"
      - "tests/grcon_mascot_success_pilot.cjs"
      - ".github/workflows/mascot-video.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate-mascot:
    name: Vídeos essenciais, piloto contextual, cache e fallback
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Instalar dependências de forma reproduzível
        run: npm ci --ignore-scripts
      - name: Gerar bundles React usados pela validação em navegador
        run: npm run build
      - run: node tests/grcon_mascot_video.cjs && node tests/grcon_mascot_runner.cjs && node tests/grcon_mascot_success_pilot.cjs
      - name: Instalar navegador de validação
        run: |
          npm install --no-save --package-lock=false playwright@1.55.0
          npx playwright install --with-deps chromium
      - name: Testar aceno, processamento, transparência visual e fallback
        run: |
          python3 -m http.server 8765 --bind 127.0.0.1 > "$RUNNER_TEMP/grcon-preview.log" 2>&1 &
          SERVER_PID=$!
          trap 'kill "$SERVER_PID"' EXIT
          node scripts/validar-mascote-video-browser.cjs
          node scripts/validar-mascote-success-pilot-browser.cjs
          node scripts/validar-consultas-browser.cjs
      - uses: actions/upload-artifact@v7
        if: always()
        with:
          name: grcon-mascot-video-validation
          path: |
            artifacts/mascot-video/*.png
            artifacts/consultas-browser/*.png
            artifacts/consultas-browser/metrics.json
          if-no-files-found: error
